""""
COMMAND TO RUN DOCKER CONTAINER WITH POSTGRES DB
""""
> ( INSTALL DBT IF NOT INSTALLED ) == > pip install dbt-core dbt-postgres dbt-redshift dbt-snowflake dbt-bigquery
# docker-compose -f docker-compose-pg-only.yml up